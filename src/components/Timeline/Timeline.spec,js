import { render, screen, fireEvent } from '@testing-library/vue'
import Card from './Card.vue'

//Render test
test('test if Card is rendering', async () => {
  render(Card);

  expect(screen.queryByTestId('card')).toBeTruthy();
})
